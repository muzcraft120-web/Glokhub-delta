-- Glok Hub v1 - Mobile Edition
-- key arka planda kontrol ediliyor, ekranda görünmüyor

local plr = game.Players.LocalPlayer
local char = plr.Character or plr.CharacterAdded:Wait()
local hum = char:WaitForChild("Humanoid")
local rs = game:GetService("RunService")
local uis = game:GetService("UserInputService")
local ts = game:GetService("TweenService")

getgenv().S = {s=16,j=50,fly=false,nc=false,fb=false}

-- GUI
local sg = Instance.new("ScreenGui", plr.PlayerGui)
sg.ResetOnSpawn = false

local main = Instance.new("Frame", sg)
main.Size = UDim2.new(0,380,0,500)
main.Position = UDim2.new(0.5,-190,0.5,-250)
main.BackgroundColor3 = Color3.fromRGB(20,20,30)
main.BorderSizePixel = 0
main.Visible = false
main.Active = true
main.Draggable = true

Instance.new("UICorner", main).CornerRadius = UDim.new(0,16)
local stroke = Instance.new("UIStroke", main)
stroke.Color = Color3.fromRGB(80,180,255)
stroke.Thickness = 3

-- başlık
local top = Instance.new("Frame", main)
top.Size = UDim2.new(1,0,0,50)
top.BackgroundColor3 = Color3.fromRGB(15,15,25)

local isim = Instance.new("TextLabel", top)
isim.Text = "Glok Hub v1"
isim.Font = Enum.Font.GothamBold
isim.TextSize = 20
isim.TextColor3 = Color3.new(1,1,1)
isim.BackgroundTransparency = 1
isim.Size = UDim2.new(1,0,1,0)
isim.TextXAlignment = Enum.TextXAlignment.Left
isim.Position = UDim2.new(0,18,0,0)

-- küçült/kapat
local min = Instance.new("TextButton", top)
min.Text = "−"; min.Size = UDim2.new(0,40,0,40)
min.Position = UDim2.new(1,-85,0,5)
min.BackgroundColor3 = Color3.fromRGB(255,180,0)
min.TextColor3 = Color3.new(1,1,1)

local close = Instance.new("TextButton", top)
close.Text = "×"; close.Size = UDim2.new(0,40,0,40)
close.Position = UDim2.new(1,-45,0,5)
close.BackgroundColor3 = Color3.fromRGB(255,70,70)
close.TextColor3 = Color3.new(1,1,1)

Instance.new("UICorner", min).CornerRadius = UDim.new(0,10)
Instance.new("UICorner", close).CornerRadius = UDim.new(0,10)

local kucuk = false
min.MouseButton1Click:Connect(function()
	kucuk = not kucuk
	ts:Create(main, TweenInfo.new(0.3), {Size = kucuk and UDim2.new(0,380,0,50) or UDim2.new(0,380,0,500)}):Play()
end)
close.MouseButton1Click:Connect(function() sg:Destroy() end)

-- key kontrol (görünmüyor)
if not getgenv().glokkey then
	main.Visible = true
	wait(1)
	sg:Destroy()
	return
end

-- özellikler buraya gelecek, ama şimdilik sade açılıyor
main.Visible = true
print("Glok Hub v1 mobile açıldı")

-- örnek özellikler (sonra doldururuz)
-- fly F, noclip H, speed/jump slider vs.
